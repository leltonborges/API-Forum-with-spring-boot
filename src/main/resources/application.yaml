spring:
  profiles:
    active: test

server:
  port: 8090

forum:
  jwt:
    secret: 3534gegfgd3645tbshdf09ubejn
    expiration: 86400000
management:
  endpoint:
    health:
      enabled: true
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*" # incluir tudo

info:
  app:
    name: @project.name@ # Nome da aplicação no pom.xml
    version: @project.version@ # Versão da aplicação no pom.xml
